### Overview

This pack contains the core workflow artifacts that serve as the foundation for building and extending integrations packs. It includes:

- `persistent streams definitions`
- `pipeline to run workflow`
- `workflow dashboards`

This Pack is designed to act as a reusable foundation for other integration packs, enabling consistent structure, easier customization, and faster development across implementations.
