%% stream = no and limit = 0

%% import_src_type = "asset-discovery"

@exec:get-input
    --> @dm:eval key="'key'"
    --> @dm:add-missing-columns columns= 'onboarding_status' and value = 'Success'
    --> @dm:eval current_time ='int(time_now_as_ms())'
    --> @dm:to-type columns="customer_tag" and type="str"
    --> @dm:manipulate-string from="current_time,customer_tag" and func="concat_columns" and to="ingestion_id"
    --> @dm:map from = 'ingestion_id' & to = 'ingestion_id' & func = 'md5'
    --> @dm:eval discovery_start_time ='utc_time_now_as_isoformat()'
    ## --> @dm:save name="test-ing-id"
    --> @dm:save name="temp-input-org-details"
    

## asset-discovery bot
--> @c:new-block
    --> @dm:recall name="temp-input-org-details" and return_empty = "yes"
    --> @dm:dedup columns = 'device_ip'
    --> *exec:if-condition onboarding_status = 'Success'
       --> @asset-discovery:collector seed_addresses_col is device_ip and collection_group_rules is "mib_discovery" and extended_inventory = True and cli_access = False and jobname = "{{row.customer_tag}}" and status_stream = "mibs_collection_status_stream" and  dedupe_serial_number = "no"
       ## --> *dm:filter collection_status = 'Success'
       --> @dm:eval key="'key'"
       --> @dm:enrich dict	 = "temp-input-org-details" and src_key_cols	 = "key" and dict_key_cols	="key" and enrich_cols	="discovery_enabled,customer_tag,customer_id,ingestion_id,discovery_start_time"
       ## --> @dm:save name="test-ing-id-enrch-1"
       --> @dm:save name = "temp-asset-discovery" & append = "yes"
    ## --> @exec:run-pipeline name ="discovery_collection" & ignore_failures = "no"
    --> @exec:end-if

--> @c:new-block
    --> @dm:recall name = "temp-asset-discovery" & return_empty = "yes"
    ## --> @dm:save name = "test-asset-discovery"
    --> @dm:dedup columns="jobid"
    --> @dm:save name="temp-jobid"

## --> @dm:save name="test-jobid"
--> @c:data-loop columns="jobid,ingestion_id,discovery_start_time,customer_tag" and dataset ="temp-jobid"
    --> @dm:empty
    --> #dm:query-persistent-stream jobid is '$jobid'
            with-input name = "mibs_collection_status_stream" & limit = "0"
    --> @dm:eval ingestion_id = "'$ingestion_id'"
    --> @dm:eval customer_tag = "'$customer_tag'"
    --> @dm:eval discovery_start_time = "'$discovery_start_time'"
    --> @dm:add-missing-columns columns = 'reason' & value = ''
    --> @dm:save name="temp-asset-status" & append = "yes"

--> @c:new-block
    --> @dm:recall name = "temp-asset-status" & return_empty = "yes"
    ## --> @dm:save name="test-asset-status"
    --> *dm:filter type_ is 'overall_status' and discovery_status is 'Completed'
    --> @dm:to_type columns="total,success,pending,failed,duplicates" & type="int"
    --> @dm:selectcolumns include="ingestion_id|discovery_start_time|total|success|pending|failed|duplicates|customer_tag|customer_id"
    --> @dm:groupby columns = 'ingestion_id,discovery_start_time,customer_tag' & agg = 'sum'
    --> @dm:copy-columns from="customer_tag" and to="jobname"
    --> @dm:rename-columns collection_start_time  = "discovery_start_time"
    --> @dm:eval collection_timestamp ='utc_time_now_as_isoformat()'
    --> @dm:eval type_ = "'overall_status'"
    ## --> @dm:save name="test-asset-query-main"
    --> @dm:save name = "temp-discovery-status" & append = "yes"
    --> @rn:write-stream name= "collection_discovery_status"
    --> @dm:recall name = "temp-asset-status" & return_empty = "yes"
    --> *dm:filter type_ != 'overall_status'
    --> @rn:write-stream name= "collection_discovery_status"

--> @c:new-block
    --> @dm:recall name = "temp-asset-status" & return_empty = "yes"
    --> *dm:filter type_ is 'device_status'
    ## --> @dm:save name = "test-asset-status"
    --> @asset-discovery:get-collection-files path_column = "minio_location"
    --> @dm:selectcolumns include = "asset_ip|groupname|output|minio_location"
    --> @dm:enrich dict	 = "temp-asset-status" and src_key_cols	 = "asset_ip" and dict_key_cols	="asset_ip" and enrich_cols	="customer_tag,ingestion_id,jobid,discovery_start_time,jobname,collection_status,reason"
    ## --> @dm:save name = "test-colection-files"
    --> @dm:explode-json column='output'
    --> @dm:selectcolumns include = "target|data|groupname|asset_ip|minio_location|customer_tag|ingestion_id|discovery_start_time|jobid|jobname|collection_status|reason"
    ## --> @dm:save name = "test-exploded-collection-files"
    --> @dm:to_type columns = 'data' & type = 'str'
    --> @rn:write-stream name= "collection_files_data"

